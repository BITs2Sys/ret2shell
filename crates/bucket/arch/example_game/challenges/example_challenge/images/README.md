# OCI image files

> delete this readme file when you have images to upload

> DO NOT DELETE THE `.gitkeep` FILE HERE

you should upload the OCI image packages here, the platform will automatically push them into the cluster private registry, and tag them with it's `<filename>:latest`.
